{
    "name": "visitor-logger-monorepo",
    "private": true,
    "type": "module",
    "workspaces": [
        "backend",
        "frontend"
    ],
    "scripts": {
        "build:frontend": "npm --workspace frontend ci --omit=dev && npm --workspace frontend run build",
        "build": "npm run build:frontend",
        "start": "node backend/server.js"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}