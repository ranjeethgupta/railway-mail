{
    "name": "visitor-logger-backend",
    "type": "module",
    "scripts": {
        "build:frontend": "cd frontend && npm ci --omit=dev && npm run build && mkdir -p ../dist/frontend && cp -r dist/* ../dist/frontend/",
        "railway-start": "npm run build:frontend && node server.js",
        "start": "node server.js"
    },
    "dependencies": {
        "express": "^4.19.2",
        "cors": "^2.8.5",
        "morgan": "^1.10.0"
    }
}